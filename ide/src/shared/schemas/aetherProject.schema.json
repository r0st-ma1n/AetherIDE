{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://aether-ide/schemas/aether-project",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "components"],
  "properties": {
    "version": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1,
      "description": "Schema version; must match CURRENT_AETHER_SCHEMA_VERSION after migrate"
    },
    "components": {
      "type": "array",
      "items": { "$ref": "#/definitions/component" }
    },
    "canvasWidth": {
      "type": "number",
      "minimum": 100,
      "maximum": 2000,
      "description": "Designer canvas width in pixels"
    },
    "canvasHeight": {
      "type": "number",
      "minimum": 100,
      "maximum": 2000,
      "description": "Designer canvas height in pixels"
    },
    "pluginType": {
      "type": "string",
      "enum": ["Effect", "Instrument"],
      "description": "Plugin category chosen in New Project wizard"
    }
  },
  "definitions": {
    "component": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "id", "x", "y", "width", "height", "properties"],
      "properties": {
        "type": { "type": "string", "enum": ["Knob", "Slider", "Button"] },
        "id": { "type": "string", "minLength": 1 },
        "x": { "type": "number" },
        "y": { "type": "number" },
        "width": { "type": "number", "minimum": 1 },
        "height": { "type": "number", "minimum": 1 },
        "properties": { "type": "object" }
      }
    }
  }
}
