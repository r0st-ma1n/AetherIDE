cmake_minimum_required(VERSION 3.15)
project({{className}} VERSION 1.0.0)

add_library({{className}} SHARED
    {{className}}.cpp
    {{className}}.h
)

target_link_libraries({{className}} PRIVATE aether_core)
target_include_directories({{className}} PRIVATE .)